import { InternalPage, internalMetadata } from "@/lib/internal-route";

export const metadata = internalMetadata("about-us-location-community");

export default function Page() {
  return <InternalPage slug="about-us-location-community" />;
}
